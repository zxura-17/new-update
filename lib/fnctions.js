/**
 * DannTeam
 * ig: @dannalwaysalone
 * Note: Jika sebar function ini, dan ketauan akan di blacklist !
 * Semua pembeli Script sudah di catat, maka berhak dia mendapatkan sanksi !
*/

var axios = require('axios');
var CFonts = require('cfonts');
var chalk = require('chalk');
var fs = require('fs');
var path = require('path');

var logoOptions = {
  font: 'tiny',
  align: 'center',
  colors: ['system'],
};

var whatsappOptions = {
  colors: ['yellow'],
  font: 'console',
  align: 'center',
};

CFonts.say('Kyuu Project', logoOptions);
CFonts.say('Instagram: @ikyrowrrr\n', whatsappOptions);
